# Termux Core Skills for OpenClaw

本技能包将 Termux 和 Termux:API 的 Android 系统能力封装为 OpenClaw 可用的标准化技能，支持摄像头控制、传感器读取、通信功能和系统 UI 交互。

## 📦 技能包结构

termux-core/
├── SKILL.md # 核心技能入口（设备控制总览）
├── camera/
│ ├── SKILL.md # 拍照/录像专用技能（含飞书优化压缩）
│ └── scripts/
│ └── capture_helper.sh # 自动化拍照+压缩脚本
├── sensors/
│ └── SKILL.md # 电池状态与传感器监控
├── comms/
│ └── SKILL.md # 短信/通话/联系人管理
├── ui/
│ └── SKILL.md # 通知/Toast/剪贴板/文件分享
└── README.md # 本安装配置指南

## ⚙️ 安装要求

### 必需组件
- **Termux** (F-Droid 或 GitHub 最新版) [下载地址](https://f-droid.org/en/packages/com.termux)
- **Termux:API** (F-Droid 安装，**不要**从 Play Store 安装) [下载地址](https://f-droid.org/en/packages/com.termux)

### 必需软件包
在 Termux 中执行：
```bash
# 安装核心API命令行工具
pkg install termux-api

# 安装图像处理工具（用于飞书照片压缩）
pkg install imagemagick

# 安装JSON解析工具（用于传感器/电池数据处理）
pkg install jq

# 验证安装
command -v termux-camera-photo && echo "✅ Termux:API 已安装"
```

### 权限配置（关键步骤）
- 打开 Termux:API 应用（不是 Termux）
- 进入 Settings → Permissions
- 根据需要启用以下权限：
    - Camera - 拍照/录像
    - Body Sensors - 传感器读取
    - SMS - 短信功能
    - Phone - 通话记录
    - Contacts - 联系人访问
    - Location - 定位功能（如需要）
    - Storage - 文件访问

⚠️ 注意：Android 系统权限和 Termux:API 权限都需要授权！

### 📱 飞书集成特别说明
由于飞书对文件大小有限制（通常 10-20MB），本技能包做了以下优化：
1. 自动压缩：capture_helper.sh 脚本会自动将照片压缩到 2048x2048 以内
2. 路径简化：所有文件路径使用 ~ 开头，减少字符长度
3. 临时文件清理：原始高分辨率照片会自动删除，节省存储空间

使用示例：
```bash
# 拍照并自动优化用于飞书
~/.openclaw/skills/termux-core/camera/scripts/capture_helper.sh -o ~/feishu_photo.jpg
```

### 📝 使用建议
- 首次使用：建议先测试 camera 和 ui 技能，验证基础功能
- 权限最小化：只开启实际需要的权限，保护隐私安全
- 路径规范：所有文件操作使用 ~/ 开头的绝对路径
- 错误处理：在自动化脚本中添加适当的错误检查和用户反馈

### 🛠 故障排除
termux-* 命令未找到  -->  执行 pkg install termux-api
权限被拒绝  -->  在 Termux:API App 中手动开启对应权限
照片上传飞书失败  -->  确保使用 capture_helper.sh 脚本，不要直接使用 termux-camera-photo
通知不显示  -->  检查 Android 系统通知设置，确保 Termux 有通知权限
magick convert 命令错误  -->  执行 pkg install imagemagick
